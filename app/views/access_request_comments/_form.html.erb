<%= form_for(@access_request_comment) do |f| %>
  <% if @access_request_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@access_request_comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @access_request_comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :access_request_id %><br>
    <%= f.text_field :access_request_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
